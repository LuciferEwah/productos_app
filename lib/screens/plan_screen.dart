import 'package:flutter/material.dart';

class PlanesScreen extends StatelessWidget {
  const PlanesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('PlanesScreen'),
      ),
    );
  }
}
